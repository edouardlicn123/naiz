# B90 — 函数索引 & Python 工具速查

快速查找：

```bash
grep -n '^void \|^int \|^uint \|^static ' core/engine/*.c core/lib/*.c core/plat/*.c | grep '('
grep -n '^void \|^int \|^static ' core/engine/nb.c | grep '('
grep -n 'cmd_table' core/engine/nb.c
```

## 一、关键文件

| 模块 | 文件 |
|---|---|
| 图层核心/生命周期 | `core/engine/layer.c` |
| 对话框/按钮样式调色板 | `core/engine/layer_dialog.c` |
| 图层背景快照 | `core/engine/layer_bg.c` |
| 场景过渡（分发） | `core/engine/transition.c` / `transition.h`（淡出 `transition_pfade.c`、百叶窗 `transition_blinds.c`、棋盘 `transition_checker.c`，共用 `transition_internal.h`） |
| 调色板原语 | `core/engine/palette.c` / `palette.h` |
| NB 脚本解释器 | `core/engine/nb.c` |
| VM 运行时状态（访问器） | `core/engine/vm.c` / `vm.h` |
| VRAM 原语 | `core/engine/render.c` |
| 文字渲染 | `core/engine/render_text.c` |
| UI 元件（圆角、浮雕） | `core/engine/ui.c` / `core/engine/ui.h` |
| 场景虚拟机/NB 解释器 | `core/engine/nb.c` |
| 主循环/初始化 | `core/engine/main.c` |
| 运行时配置 | `core/engine/settings.c` / `settings.h` |
| 精灵表/表达式映射 | `core/engine/nb_asset_table.h` |
| 存档槽位 | `core/engine/save.c` |
| 存档 I/O 公共原语 | `core/engine/save_io.c` |
| 系统存档（解锁） | `core/engine/save_sys.c` |
| 全屏存档/读档菜单 | `core/engine/nb_saveload.c` |
| 对话框内保存菜单 | `core/engine/nb_save_dialog.c` |

## 二、C 函数索引

### 入口函数

- `main()` → `main.c`
- `nb_init()` / `nb_process()` / `nb_load()` → `nb.c`
- `nb_is_menu_scene()` / `nb_get_state()` / `nb_set_lang()` — NB 状态访问器 → `nb.c`（nb_is_menu_scene 查 sceneconf 声明的场景类型 == menu）
- `settings_load()` / `settings_get_version()` 等 getter → `settings.c`

### 场景生命周期 → `layer.c` / `layer_bg.c` / `layer_dialog.c` / `layer_sprite.c`

- `layer_init()` / `scene_end()` — 初始化/清理 → `layer.c`
- `layer_capture_bg()` / `layer_capture_bg_dialog()` / `layer_capture_bg_dialog_from_bg()` — 背景捕获 → `layer_bg.c`
- `layer_bg_reset()` — 背景快照释放/清标志（layer_internal.h）→ `layer_bg.c`
- `layer_bg_restore_rect()` / `layer_bg_snapshot_valid()` / `layer_bg_snapshot()` / `layer_bg_dialog_snapshot()` — 背景访问器（layer_internal.h）→ `layer_bg.c`
- `layer_dialog_open()` / `layer_dialog_restore()` / `layer_dialog_snap()` / `layer_dialog_hide()` — 对话框管理 → `layer_dialog.c`
- `layer_dialog_drawn()` / `layer_dialog_refresh()` / `layer_dialog_mark_dirty()` / `layer_dialog_clear()` / `layer_dialog_reset()` / `layer_dialog_snapshot()` — 对话框状态/访问器 → `layer_dialog.c`
- `layer_sprite_show()` / `layer_sprite_face()` / `layer_sprite_replace()` / `layer_sprite_hide_all()` / `layer_has_sprite()` — 精灵管理 → `layer_sprite.c`
- `layer_redraw_sprites()` — 重绘所有精灵 → `layer_sprite.c`
- `layer_any_sprite()` / `layer_sprite_hide()` / `calc_sprite_clip_h()` / `sprite_entry_update()` / `find_sprite()` / `alloc_sprite()` — 精灵内部静态辅助 → `layer_sprite.c`
- `dialog_snapshot_capture()` / `scene_draw_dialog()` — 对话框内部静态辅助 → `layer_dialog.c`
- `dlg_update_palette()` / `btn_update_palette()` / `dlg_set_style()` / `dlg_get_style()` / `btn_set_style()` — 调色板对话框样式 → `layer_dialog.c`
- `transition_run()` → `transition.c`
- `palette_get_all()` / `palette_set_all()` / `palette_interp()` — 调色板全表读写/插值原语（供转场淡出与 NAIZ_ANIM 调色板轨复用）→ `palette.c`

### 光标绘制 → `cursor.c`

- `cursor_render()` / `cursor_render_force()` — 光标绘制（save/restore 背景，VBLANK 同步）
- `cursor_erase_current()` / `cursor_invalidate()` — 光标擦除/失效（呈现代理，被 hal_mouse 转发调用；`mouse.c` 不再含任何光标绘制代码）

### 渲染原语 → `render.c` / `ui.c`

- `vram_pset_addr()` / `fill_rect()` / `draw_rect()` — 基本像素操作
- `fill_diag_sweep()` — 对角百叶窗条带单趟填充（bank 跨整帧跟踪；dblinds/rdblinds 转场用）
- `draw_text()` / `text_width()` / `draw_title_large()` — 文字渲染
- `vram_blit()` / `vram_blit_sprite()` / `fill_rect_pattern()` — 图像 blit
- `vram_row_copy()` (内部静态) — 不透明 blit 的 bank 段批量拷贝（`rep movsb` 快路径；逐字节 volatile 存储在解释执行模拟器下全屏需 ~1s，此路径降至瞬时。0.1.006 性能修复）
- `vram_read()` / `vram_write()` — VRAM 批量读写
- `draw_rounded_emboss()` / `draw_rounded_rect()` — 圆角矩形（菜单/按钮）→ `ui.c`
- `draw_glyph_internal()` (内部静态函数，`draw_ascii`/`draw_cjk` 宏包装)
- `vblank_wait()` — 垂直回扫等待

### 对话框 & NB 系统 → `nb.c` / `nb_parser.c` / `nb_commands.c` / `nb_audio.c` / `nb_mainmenu.c` / `nb_dialog.c` / `nb_menu.c` / `nb_vars.c`

- `dialog_show()` / `nb_dialog_get_text()` / `nb_dialog_set_text()` → `nb_dialog.c`
- `nb_commands_dispatch()` — NB 命令调度入口 → `nb_commands.c`
- `nb_asset_id()` — 资产 key→ID（IMG 优先，SPR 兜底，供标题黑花 MAG 用）→ `nb_commands.c`
- `nb_lang_is_cjk()` / `text_set_blackletter()` — 黑花体语言判定与开关 → `nb.c`（setter 实现在 `render_text.c`）
- `nb_parse_line()` / `nb_get_line()` — 脚本行 CSV 解析/取行 → `nb_parser.c`
- `nb_parse_line_semi()` — 脚本行 `;` 顶层分隔解析（question/scene 多段命令，段内 `,` 保留供 `nb_next_field` 二次消费）→ `nb_parser.c`
- `nb_set_scene_conf()` / `nb_set_last_choice()` / `nb_get_filename()` / `nb_get_buffer()` — NB 状态访问器（`NbState` 为 nb.c 私有）→ `nb.c`
- `cmd_bg()` / `cmd_char()` / `cmd_host()` / `cmd_dialogue()` / `cmd_scene()` / `cmd_question()` / `cmd_loadscene()` / `cmd_sceneconf()` — NB 命令处理 → `nb_commands.c` (cmd_scene → `nb_scene.c`, cmd_question → `nb_question.c`, cmd_loadscene → `nb_saveload.c`)
- `cmd_bgm()` / `cmd_sound()` / `cmd_voice()` — 音频命令 → `nb_audio.c`
- `cmd_mainmenu()` / `cmd_settingmenu()` / `cmd_cgvmenu()` / `cmd_musicmenu()` — 菜单命令 → `nb_mainmenu.c`
- `save_dialog_menu()` / `save_load_draw()` — 存档/读档菜单 → `nb_saveload.c`
- `menu_show()` / `menu_save_item_palette()` / `menu_restore_item_palette()` / `menu_consume_key()` → `nb_menu.c`
- `nb_var_init()` / `nb_var_get/set/add()` / `nb_var_lookup()` / `nb_var_get_state/set_state()` → `nb_vars.c`
- 核心调度链路:
  `nb_load()` (nb.c:148) → 加载 .nb，重置 VM，`kbd_drain_advance()` + `kbd_set_ignore_frames(2)`
  `nb_process()` (nb.c:243) → VM 主循环；入口 `anim_waiting()` 守卫（动画等待时暂停脚本推进）
  `nb_commands_dispatch()` → 命令分发（`nb_commands.c`）
  `cmd_scene()` → 场景跳转（`nb_scene.c`）
  `cmd_playanima()` → 动画播放（`nb_anim.c:286`）
  `cmd_waitanima()` → `vm_pause_process()`（`nb_anim.c:522`）
  `main.c:118/136` → 外层循环每帧调 `anim_tick()`；内层等待循环也调 `anim_tick()`

### VM 运行时状态 → `vm.c`（访问器声明于 `vm.h`）

- `vm_request_process()` / `vm_pause_process()` — 请求/暂停处理
- `vm_set_error()` / `vm_clear_error()` — 错误标志
- `vm_set_finalend()` — 引擎退出标志
- `vm_request_scene_change()` / `vm_clear_scene_change()` — 场景切换
- `vm_get_flags()` — 读取 VM 状态位（`VMFLAG_*`）
- `vm_delay_reset()` / `vm_delay_tick()` / `vm_delay_active()` — 帧延迟计数器
- 状态定义：`vm_flags` / `delay_frames` 均为 vm.c 私有 `static`，外部经访问器读写

### 图片管理 → `image.c` / `image_cache.c`

- `image_init()` / `image_load()` / `image_raw_blob()` / `image_set_palette()` / `image_get_entry()` — 图片归档 → `image.c`（`image_raw_blob` 零拷贝返回归档内原始字节，供 .ANI 容器直读）
- `image_cache_init()` / `image_cache_clear()` / `image_cache_lookup()` / `image_cache_insert()` — LRU 缓存（8 槽，引用计数免深拷贝；insert 转移所有权，lookup 返回 retain 共享指针）→ `image_cache.c`（内部接口见 `image_internal.h`）

### ANI 动画播放 → `nb_anim.c`

- `cmd_playanima()` / `cmd_waitanima()` / `cmd_stopanima()` — NB 动画命令（注册于 nb_commands.c cmd_table）
- `anim_tick()` — 每帧推进动画（main.c 主循环调用）；`anim_waiting()` — waitanima 暂停态查询；`anim_stop()` — 幂等停止（cmd_bg/scene_end 隐式停止入口）
- 内部：`anim_stop_internal()` / `anim_decode_frame()` / `anim_draw_frame()` / `anim_frame_ticks()` / `anim_rebuild_dialog_if_open()`；状态 `AnimState g_anim`（头文件 `nb_anim.h`）
- 调用链路（→ 表调用，← 表被调用）:
  `nb_load()` → `nb_process()` → `anim_waiting()` ← `cmd_waitanima()` → `vm_pause()`
  `cmd_playanima()` (nb_anim.c:286) → `anim_start()` → arm state
  `main.c:118/136` → `anim_tick()` (nb_anim.c:565) → `anim_draw_frame()` (nb_anim.c:208) → `vram_blit()`

### 存档系统 → `save.c` / `save_io.c` / `save_sys.c`

- `save_game_temp()` / `save_game_slot()` — 存档写入 → `save.c`
- `load_game_temp()` / `load_game_slot()` — 存档读取 → `save.c`
- `slot_info()` / `save_get_filename()` — 槽位查询 → `save.c`
- `save_file_write()` / `save_file_read()` — 公共 I/O 原语（magic/version/checksum 校验；checksum 偏移由调用方以 `offsetof(..., checksum)` 传入，兼容 SaveData@60 / SystemSave@8） → `save_io.c`（`save_checksum()` 已 static 化：仅 save_io.c 内部使用）
- `sys_save_load()` / `sys_save_unlock_scene()` — 跨模块系统存档 API → `save_sys.c`
- `sys_save_write()` / `sys_save_unlock_cg()` / `sys_save_is_cg_unlocked()` / `sys_save_unlock_ending()` / `sys_save_is_ending_unlocked()` / `sys_save_inc_clear_count()` / `sys_save_is_scene_unlocked()` — save_sys.c 内部 `static`（原 `save_sys_internal.h` 已删除）

### 平台层键盘 → `keyboard.c`

- `kbd_init()` / `kbd_update()` — 初始化/轮询
- `kbd_is_down()` — 按键状态查询（消耗）
- `kbd_flush()` / `kbd_drain_advance()` — FIFO 清理
- `kbd_wait_any()` — 阻塞等待（`kbd_is_pressed` / `kbd_wait_confirm` 已删除：无调用方；`kbd_wait_release` 已 static 化：仅 keyboard.c 内部 `kbd_drain_advance` 使用）
- `kbd_set_ignore_frames()` — 忽略帧设置

### 平台层鼠标 → `mouse.c`

- `mouse_init()` — 三路初始化（8255 + INT33 检测 + NP2 系统端口检测）
- `mouse_update()` — 三路更新（NP2 端口 > INT33 > 8255 回退）
- `mouse_np2_detect()` / `mouse_np2_getpos()` — NP2 检测/绝对坐标
- `mouse_get_x()` / `mouse_get_y()` — 坐标访问器
- `mouse_available()` / `mouse_get_display_x()` / `mouse_get_display_y()` — 可用性/显示坐标访问器（供 cursor.c 呈现驱动器读取）
- `mouse_was_clicked()` — 按钮点击状态（`mouse_is_down()` 无损调用方：已被删除的 `hal_mouse_is_down` 包装是唯一使用者）
- `mouse_flush()` — FIFO 清理（`mouse_is_available()` 已删除：无调用方）
- `mouse_set_pos()` / `mouse_drain()` / `mouse_recenter_if_idle()` — 位置管理
- 光标呈现代理已移至 `cursor.c`；`hal_mouse.c` 将 `hal_mouse_draw_cursor()` 等转发到 `cursor_render*()`

### 平台层串口 → `serial.c`

- `serial_init()` / `serial_puts()` / `serial_puthex()` / `serial_rx_on()` — 调试通道（`serial_getc()` 已删除：无调用方）

### 平台层视频 → `video.c`

- `video_init()` / `video_check_palette()` / `video_exit()` — 模式设置/调色板自检

### 平台层 GDC → `gdc.c`

- `gdc_init()` / `gdc_set_palette()` / `gdc_read_palette()` — GDC 调色板 I/O

### 平台层 VRAM → `vram.c`

- `vram_init()` / `vram_plane()` — VRAM 线性地址映射

### 平台层 HAL → `plat/hal_*.c`

- `hal_init()` / `hal_log()` — 平台初始化/调试日志（`hal_pc98.c`）
- `hal_set_palette()` / `hal_read_palette()` — 调色板（`hal_pc98.c`）
- `hal_vblank_wait()` — 垂直同步等待（GDC 0xA0 bit5 VSYNC；历史 bug：曾误用 bit2 FIFO EMPTY 导致无节拍，0.1.008 修复）（`hal_pc98.c`）
- `hal_wallclock_ms()` — 墙钟毫秒（DOS INT 21h AH=2Ch，10ms 粒度；时间基动画步进的时源，与帧率解耦）（`hal_pc98.c`）
- `hal_vram_bank_select()` / `hal_vram_get_window()` — VRAM bank 窗口（`hal_pc98.c`）
- `hal_bgm_play/stop()` / `hal_sound_play()` / `hal_voice_play()` — 音频控制（`hal_pc98.c`）
- `hal_kbd_*()` — 键盘转发（`hal_kbd.c`）
- `hal_mouse_*()` — 鼠标转发（`hal_mouse.c`）
- `hal_video_*()` — 视频转发（`hal_video.c`）

### 共享工具 → `lib/`

- `file_read_all()` → `naiz_file.c`（文件读取）
- `font_init()` / `font_get_glyph()` / `font_load_alt()` / `font_get_glyph_alt()` → `font.c`（8×16 字形 + BLACK.DAT 16×16 黑花体备选表）
- `cjk_init()` / `cjk_get_glyph()` → `cjk.c`（CJK 字形）
- `mag_decode()` / `mag_read_palette()` / `mag_retain()` / `mag_release()` → `mag.c`（MAG 解码；引用计数管理，`mag_release` 归 0 释放）
- `tr_init()` / `tr()` / `tr_get_count()` → `tr.c`（翻译模块）

## 三、Python 工具入口

| 工具 | 模块路径 | 功能 |
|---|---|---|
| 注入 HDI | `tools.naiz_img.inject` | HDI 增量注入 |
| 构建全流程 | `tools.naiz_build.build_game` | build 主流程 |
| NB 脚本检查 | `tools.naiz_build.nb_validator` | .nb 语法验证 |
| 图片打包 | `tools.naiz_build.pack_images` | MAGs → IMAGE.DAT；`load_img_map_assets(project_dir, types=None)` 为 img_map 装载公共入口（读行+解码降级，anim_import.derive_shared_palette 共用） |
| PNG→MAG | `tools.naiz_conv.mag_convert` | 素材转换 |
| 资产表导出 | `tools.naiz_build.export_asset_table` | 生成资产表 C 头文件 |
| 变量表导出 | `tools.naiz_build.export_vars` | 生成 nb_var_table.h（宏+typedef）与 nb_var_defs.h（数据表） |
| 配置导出 | `tools.naiz_build.export_config` | config.toml → nb_config.h（转场类型/帧数） |
| C 头样板 | `tools.naiz_build.c_header` | 生成器共享样板（escape/preamble/footer） |
| 工程配置 | `tools.naiz_build.project_config` | config.toml 统一只读访问器（build_game/export_config/i18n_gen/bump_version 共用） |
| 版本号自增 | `tools.naiz_build.bump_version` | config.toml version 最右段 +1 |
| 封装/拆分审计 | `tools.diag.symbol_audit` | 单次扫描 core/ 输出 A=static 候选 / B=死导出 / C=耦合（含行数）/ D=符号清单 / E=拆分簇前缀聚类（`-s A,E` 选节）；`start.sh audit` 生成时间戳日志并输出终端 |
| 规则审计引擎 | `tools.audit.audit` | 按 AGENTS §十七 固化审计：C/P/S 规则扫描 + sha256 增量（git 增量记录在 audit_state.json）；`--reset` 全量 / `--rules C5,C11` 定点 / `--since <git-ref>` 新代码门禁（仅审计变更行，新违规 exit 1）/ `--no-save` / `--no-manual` / `-v` / `-q`；确定性违规 exit 1，启发式候选仅报告；P 规则为 AST 级、C1/C4/C14 为函数分段窗口化；RESULT 输出"状态 + 全量 Verification 两行" |
| 规则定义 | `tools.audit.rules_c/rules_p/rules_s` | C1-C15+C21-C25/P1-P10+P14/S1-S8 规则实现 + MANUAL_NOTES 人工清单（C6/C7/C9/C16-C20、P4/P11、S7）；C21/P14 为 AUTO 黑名单（strcpy/strcat/gets、eval/exec/os.system/os.popen）；C22/C23/C24 为 AUTO（INT_MIN 取负/同路径双 free/memcpy 尺寸交叉核对），C25 为 HEUR（use-after-free） |
| 核验登记 | `tools.audit.verify` | 独立 verify_notes.json（v2 行级）：`--note REL:LINENO:VERDICT[:TEXT]`（自含 spec，可重复）记录 AI/人工核验结论，存**行级文本快照**（被核行未变则无关编辑不 STALE，v1 sha8 整文件快照兼容加载）；`--list-notes` 查看；候选行打印 `[verified-*]` 标记，不抑制条目录入 |
| 审计状态 | `tools.audit.state` | audit_state.json 读写（version/git_head/sha256/violations/findings 明细，v2） |
| FAT16 浏览 | `tools.diag.read_fat16` | FAT 分区浏览 |
| HDI AUTOEXEC 补丁 | `tools.diag.hdi_patch_autoexec` | 修改 AUTOEXEC.BAT |
| 串口调试 | `tools.diag.np2kai_serial` | PTY 管道 + 日志 |
| NP2kai 捕获共享 | `tools.naiz_lib.np2kai_capture` | 窗口发现/截图/启动/TOML 写入 `write_emulator_toml`（供 screendig、serial、env_test 共用） |
| NB 行解析共享 | `tools.naiz_lib.nb_line` | NB 命令行统一解析（nb_validator 与 i18n_gen 共用） |
| ANI 容器编解码 | `tools.naiz_lib.anim_container` | .ANI v1 格式权威实现：`build_ani`/`parse_ani`/`mag_blob_length`（L1-L5 校验；逐帧 tick 表；devdoc 78 §4） |
| 动画项目架构 | `tools.naiz_build.anim_project` | animation/projects/<名>/{config.toml,scripts/,db/} 单一事实源：`load_project`（name=目录名校验）/`iter_projects`/`scaffold`/`scripts_dir_for` 等路径推导；CLI `init`/`list` |
| 动画脚本解析 | `tools.naiz_build.anim_script` | 动画脚本(.na)解析（animaconf/frame/base/pal 裸名字语法，F1 后缀门禁 + V1-V8 校验 + 项目动画库查名）+ `parse_pal_file` pal 差异文件校验 |
| 动画素材登记 | `tools.naiz_build.anim_register` | 扫描 `assets/<项目>/anim/` 登记/同步 `animation/projects/<项目>/db/<项目>.db` 名字索引（前置项目架构校验；差量事务）；只读对账 `diff_project`（anima.sh register/check 入口，--check 有差异 exit 1） |
| 动画组装 CLI | `tools.naiz_build.anim_import` | 动画脚本(.na) → .ANI 组装（裸名查库取素材；`--project` 一致性校验；`--sync` 构建前同步登记库；anima.sh 入口） |
| COM 生成 | `tools.diag.gen_com` | 预设 COM 文件生成 |
| 屏幕调试 | `tools.naiz_screendig` | 持续/单次截图分析 |
| MAG 转换 | `tools.naiz_conv.mag_convert` | 完整 MAG 转换（PNG→MAG 唯一入口） |
| PSF→FONT | `tools.naiz_conv.psf2font` | PSF 字体→FONT.DAT 转换（共享 `build_font_dat`） |
| TTF→16×16 FONT | `tools.naiz_conv.ttf2font` | TTF→BLACK.DAT（16×16 黑花体字形，共享 `build_font_dat`） |
| FONT.DAT 容器共享 | `tools.naiz_lib.font_dat` | MHVN98 FONT.DAT 容器构建（psf2font/ttf2font 共用） |
| 黑花标题 MAG | `tools.naiz_conv.render_title` | LOAD/SAVE→精灵 MAG（loadtitle/savetitle） |
| 国际化生成 | `tools.naiz_conv.i18n_gen` | .po → 翻译表生成 |
| 环境安装 | `tools.env_setup.install_env` | 入口：main/argparse/交互菜单 |
| | `tools.env_setup.env_utils` | 共享工具（日志/配置/Git/包管理器/补丁检查） |
| | `tools.env_setup.env_toolchains` | 工具链命令（pip/Watcom/DJGPP/gcc-ia16） |
| | `tools.env_setup.env_np2kai` | 模拟器命令（NP2kai/libretro/deps/补丁；`_apply_common_patches` 应用 `tools/np2kaipatch/*.patch`，`cmd_np2kai` 支持 `NAIZ_NP2KAI_REBUILD` 强制重编译） |
| | `tools.env_setup.env_build` | re-export 兼容层（自 env_toolchains + env_np2kai） |
| | `tools.env_setup.env_test` | 测试命令（test-hdi/check/retroarch；wxnp21kai.toml 写入复用 `np2kai_capture.write_emulator_toml`） |
| CJK 字体生成 | `tools.naiz_font.gen_cjk_font` | hex→CJK.DAT 生成 |
| HDI 文件列表 | `tools.naiz_img.listhdi` | 列出 HDI 内文件 |
| HDI 提取 | `tools.naiz_img.extract` | 从 HDI 提取文件到宿主机 |
| 文件注入 | `tools.naiz_img.inject_file` | 灵活参数文件注入 |
| 文档构建 | `tools.naiz_docs.build_docs` | 文档站构建 |

## 四、相关参考

| 文档 | 内容 |
|------|------|
| `docs/B91-构建环境与参考速查.md` | 构建环境/运行时约束/架构边界/关键参考 |
| `docs/B92-NB脚本命令参考.md` | NB 命令表（唯一集中源）+ 关键常量速查 |
| `docs/B02-显示管线规范.md` | 显示管线/VRAM/调色板完整规范 |
