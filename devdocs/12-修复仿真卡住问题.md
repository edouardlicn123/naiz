# 修复仿真卡住问题的计划

## 背景
最近的日志显示在模拟器启动后，忽略了在 **显示空闲内存** 时出现卡住的现象。结合 **10** 与 **11** 的修复方案，可知卡住主要源于：

1. **引擎缺少自动退出机制**。
2. **启动脚本仍使用旧的 `SDL_VIDEODRIVER` 并且超时 60 s**。
3. **BIOS/ROM/VBR 并未真正与最终可执行文件协同**。

## 目标
- 在引擎层实现 300 帧（≈ 5 s）自动退出。
- 将启动脚本调整为 **11** 中要求的形式。
- 验证 BIOS、VBR、DOS 软链均正常。
- 重新编译、注入、运行并确认模拟器在不超过 15 s 内完成整个流程。

## 步骤
1. **检索并备份现有文件**（避免覆盖）
   - 备份 `core/engine/main.c` 成 `core/engine/main.c.bak`。
   - 备份 `testtemp.sh` 成 `testtemp.sh.bak`。
2. **更新 `core/engine/main.c`**
   - 用下列代码替换，满足文档 10 的帧计数器退出逻辑。
   ```c
   /* 引擎主函数：循环 300 帧后自动退出 */
   #include <stdint.h>
   #include "plato.h"   /* 假设有相关 HAL 声明 */
   
   int main(void) {
       uint32_t frame = 0;
       while (frame < 300) {
           hal_video_draw();   /* 假设存在此函数绘制图形 */
           hal_input_poll();   /* 处理输入 */
           frame++;
       }
       return 0;   /* 退出 */
   }
   ```
   （注：具体绘图/输入函数名按项目实际接口调整）

3. **更新 `testtemp.sh`**
   - 去掉 `export SDL_VIDEODRIVER=x11`。
   - 将 `timeout 60` 改为 `timeout 15`。
   - 保留 `nice -n 10` 与 `SDL_VIDEO_ALLOW_SCREENSAVER=1`。
   - 示例：
   ```bash
   #!/bin/bash
   set -e
   ...
   export SDL_VIDEO_ALLOW_SCREENSAVER=1
   export SDL_AUDIODRIVER=dummy
   export PULSE_SERVER=0
   nice -n 10 timeout 15 sdlnp21kai_sdl2 2>&1 | tee -a "$LOG"
   ```

4. **确认 BIOS / VBR / DOS 链接**
   - 检查 `tools/pc98bios/ROM.ROM` 是否存在且大小为 98304 B。
   - 打开 `tools/hdi_tool/hdi_img.py`，确认 `vbr[0x1FE:0x200] = b'\x55\xAA'` 已写好。
   - 确认 `[PC98] MS‑DOS6.2` 软链指向 `/home/edo/[PC98] MS‑DOS6.2`。

5. **重新编译与注入**
   ```bash
   cd /home/edo/naiz
   make -C core all
   ./testtemp.sh
   ```
   - 观察日志：
     * 先出现 `=== Build ===`、`=== Inject ===`。
     * 然后模拟器启动、DOS boot、ENGINE 运行，**5 s 后自动回到 DOS**。
     * 最后超时（15 s）退出，`exit=0`。

6. **验证**
   - 确认 `np2kai-<时间>.log` 没有 `exit=124`。 
   - 确认屏幕不再全黑，且 DOS 提示符可见。
   - 确认 VM 输入（键盘/鼠标）仍能交互。

## 故障排查
- 若仍卡住，检查 `np2kai.cfg` 是否指向正确 BIOS 目录；
- 查看 `np2kai-*.log` 中是否有 *MESA* 或 *GLX* 错误，必要时切换 `SDL_VIDEODRIVER=x11`；
- 确认 `core/engine/main.c` 中帧计数器的逻辑与渲染周期一致；
- 在 log 中搜索 `frame` 或 `timeout` 关键字，定位异常位置。

## 备注
- 本计划文件位于 `docs` 目录，方便后续维护与审阅。
- 如果你在实施过程中遇到编译错误，请先确认头文件路径与命名一致，再进行调试。

*本文件由系统自动生成，旨在帮助你完成修复仿真卡住问题的完整步骤。*
