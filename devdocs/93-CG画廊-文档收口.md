# 93-CG画廊-文档收口

> CG 画廊功能阶段 7/7（最终阶段）。本阶段仅做文档收口，零代码改动。
> 记录 B92/B90 参考文档的收口改动明细、版本 bump、devdoc 89 脱节点的最终澄清。

## §一 阶段定位

CG 画廊功能共 7 阶段（devdoc 88/89/90/91/92/93）：

| 编号 | 阶段 | 类型 | 状态 |
|------|------|------|------|
| 88 | CG 数据管线（type='CG'） | 实现 | ✅ |
| 89 | CG 解锁存档 API | 实现 | ✅ |
| 90 | cmg_cg 展示命令 | 实现 | ✅ |
| 91 | 画廊入口骨架与主菜单桥接 | 实现 | ✅ |
| 92 | 网格 UI + 全屏预览 + 占位框 | 实现 | ✅ |
| **93（本档）** | **文档收口** | **文档** | ✅ 本档 |

本阶段收口对象：

1. `docs/B92-NB脚本命令参考.md` — `cg` 命令、`cgvmenu` 命令、数据管线六表描述
2. `docs/B90-参考-函数索引.md` — `cmd_cgvmenu`、`cmd_cg`、`sys_save_unlock_cg`/`sys_save_is_cg_unlocked` 的"内部 static"过期描述修正
3. 版本 bump（已在阶段 5/6 落地为 0.2.077，本阶段零代码故不改版本）

## §二 B92 收口明细

### 2.1 命令表

- **新增 `cg` 行**（命令表中排在 `settingmenu` 与 `cgvmenu` 之间）：

  ```
  | `cg` | `cmd_cg` (nb_cg.c) | `cg <asset_key>` | 展示 CG（type='CG' 资产，用法同背景图），绘制后永久解锁该 CG 至 SYSTEM.SAV |
  ```

  依据：devdoc 90 阶段 3 落地了 `cmd_cg`，但 B92 命令表当时未同步；本阶段补录。

- **修订 `cgvmenu` 行**（从 TODO 改为已实现）：

  ```
  | `cgvmenu` | `cmd_cgvmenu` (nb_mainmenu.c) | — | 打开 CG 画廊（由 cgview.nb 调用）：网格浏览 + 锁定占位 + 翻页 + 全屏预览，ESC/Back 回主菜单 |
  ```

  依据：devdoc 91/92 落地后不再是 TODO 桩；实现模块从 `nb.c` 更正为 `nb_mainmenu.c`（`cmd_cgvmenu` 实际定义于 nb_mainmenu.c）。

### 2.2 数据管线描述

- `docs/B92` §3 管线段第 115 行原描述：

  ```
  ASSETS.DB → export_asset_table.py → nb_asset_table.h（asset/spr/char/expr/anim_map 五表）
  ```

  收口为：

  ```
  ASSETS.DB → export_asset_table.py → nb_asset_table.h（asset/spr/char/expr/anim/cg_map 六表 + CG_COUNT 常量）
  ```

  依据：devdoc 88（type='CG' 生成 `cg_map[]`）+ devdoc 92（追加 `#define CG_COUNT`）。

## §三 B90 收口明细

### 3.1 `cmd_cgvmenu` 描述

- 原行（函数索引菜单命令段）仅列命令名，未标注实现状态。
- 收口后：

  ```
  cmd_mainmenu() / cmd_settingmenu() / cmd_cgvmenu() / cmd_musicmenu() — 菜单命令 → nb_mainmenu.c
  （cmd_cgvmenu 为 CG 画廊网格 + 全屏预览；cmd_settingmenu/cmd_musicmenu 仍为 TODO 桩）
  ```

### 3.2 `cmd_cg` 函数索引

- 原 B90 NB 命令处理段未列 `cmd_cg`（阶段 3 落地后遗漏）。
- 收口后在该段新增一行：

  ```
  cmd_cg() — CG 展示命令（type='CG' 资产，同背景渲染路径 + 永久解锁） → nb_cg.c
  ```

### 3.3 `sys_save_unlock_cg` / `sys_save_is_cg_unlocked` 脱节点修正

- **原 B90 行**（line 132–133 区域）：

  ```
  sys_save_write() / sys_save_unlock_cg() / sys_save_is_cg_unlocked() / ... — save_sys.c 内部 static（原 save_sys_internal.h 已删除）
  ```

  这是**过期描述**：devdoc 89 阶段 2 已将它们实现为**公开非 static**、声明于 `save.h`，供 `cmd_cg` / `cmd_cgvmenu` 跨文件调用。

- **收口后**：从"内部 static"清单拆分出 `sys_save_unlock_cg` / `sys_save_is_cg_unlocked`，归入跨模块公开 API 段：

  ```
  sys_save_unlock_cg() / sys_save_is_cg_unlocked() — CG 解锁/查询（cg_id 为 1-based，越界静默 return；画廊 cmd_cgvmenu 与 cmd_cg 消费） → save_sys.c（公开非 static，save.h 声明）
  ```

  其余（`sys_save_unlock_ending` / `is_ending_unlocked` / `inc_clear_count` / `is_scene_unlocked` / `sys_save_write`）仍为内部 static，保留在原段。

## §四 devdoc 89 脱节点的最终澄清

<子系统追溯，避免后续误读>

- devdoc 89 §2.3（行 67–68）在阶段 2 落地前已**正确披露** B90 的"内部 static"描述是过期残留记录，并预告"阶段 7 文档收口时同步 B90"。这是如实披露，非错误声称。
- devdoc 89 第 69 行另预告"`docs/B18-存档读档系统设计.md` §2.4 表格列 `sys_save_unlock_*(id) | 290–333` 行号区间描述同样过期，阶段 7 修正"。
- devdoc 89 行 95–100 是 API 的**实现预览**（正确的函数实现），同样无错误。
- devdoc 92 §九 初稿曾写"修正 devdoc 89 第 98–100 行对 is_cg_unlocked 已实现的误导性脱节说明"——此为不准确表述（devdoc 89 本身无错误，脱节点仅在 B90）。已在阶段 5 实现时以注释形式订正。
- **结论（初版）**：脱节点唯一存在于 `docs/B90`。经复查发现**此结论不完整**——B18 的脱节点（行 110 区间描述 + 未列新增 CG 解锁函数）在初版收口时被遗漏，本次审查后已补修（见 §六 变更清单）。修订后脱节点共两处：`docs/B90`（§3.3 已修）与 `docs/B18`（§五新增节已修）。

## §五 B18 收口明细（复查补修）

> 初版收口遗漏；本次全文档复查时据 devdoc 89 第 69 行预告补修。

### 5.1 变更

`docs/B18-存档读档系统设计.md` §2.4 表格原为：

```
| `sys_save_load()` | 272 | 启动时加载 `SYSTEM.SAV` |
| `sys_save_write()` | 284 | 写系统存档 |
| `sys_save_unlock_*(id)` | 290–333 | 设位 + 即时写盘 |
```

问题：
1. 行号区间 `290–333` 已过期——`save_sys.c` 现行号远小（`sys_save_load`=34 / `sys_save_write`=45 / `unlock_scene`=50 / `unlock_cg`=58 / `is_cg_unlocked`=66）。
2. 未列阶段 2 新增的 `sys_save_unlock_cg` / `sys_save_is_cg_unlocked`。

收口为逐行精确表：

```
| `sys_save_load()` | 34 | 启动时加载 SYSTEM.SAV |
| `sys_save_write()` | 45 | 写系统存档（static，置位后即时写盘） |
| `sys_save_unlock_scene(scene_id)` | 50 | 设 scene_flags 位 + 即时写盘（越界静默 return） |
| `sys_save_unlock_cg(cg_id)` | 58 | 设 cg_flags 位 + 即时写盘（1..CG_TOTAL，越界静默 return） |
| `sys_save_is_cg_unlocked(cg_id)` | 66 | 查询 cg_flags 位（CG 画廊网格用） |
```

### 5.2 devdoc 92 偏差记录（实现 vs 文档）

全文档复查还发现 devdoc 92 与实际实现存在以下**描述偏差**，均为文档滞后于实现的记录性问题（非功能 bug，功能已由 make/fullaudit 验证）：

| devdoc 92 描述 | 实际实现 | 修正建议 |
|------|-----------|---------|
| §四.B "3 个 static 辅助函数"（cell/grid/cells_range） | 实际 **6 个**：`gallery_cell_xy`、`gallery_draw_cell`、`gallery_draw_grid`、`gallery_draw_cells_range`、`gallery_preview`、`gallery_exit_preview` | 更正计数与函数清单 |
| §四.B `gallery_draw_cells_range` 伪码含"静态占位数组 old_idx[2]/new_idx[2] + ..." | 实现以 `from_sel`/`to_sel` 直接驱动，无占位数组 | 删除占位伪码 |
| §四.B cmd_cgvmenu 主体把 preview 逻辑内联 | 实际抽为 `gallery_preview()`/`gallery_exit_preview()` 两函数 | 更正结构描述 |
| §四.B 预览焦点恢复依赖 `prev_sel` | 实际靠 `view` 切换期间保持 `sel/page/focus_on_back` 不变，退出预览直接用当前值（`gallery_exit_preview`） | 更正恢复机制说明 |
| §七 验证表格步骤 h"再进 nbook001 打到 cg(cg01) 触发第二次解锁" | `nbook001.nb` 仅插一次 `cg(cg01)`，无第二次解锁；且解锁已由步骤 i 覆盖 | 改为仅"检查 SYSTEM.SAV cg_flags 确认 cg_id=1 置位、cg02 未置位" |

上述并非 doc 侧需要回改实现，而是 devdoc 92 作为"阶段 5 设计预期"与最终实现存在演化差异——devdoc 92 处于"活动文档"状态，如需与实现严格对齐可修订，但按 devdoc 惯例（记录设计意图）保留亦可。

## §六 版本记录

- 阶段 5/6 实现改动代码（`nb_mainmenu.c`、`export_asset_table.py`）+ 新增 `cgview.nb` → 版本已 bump：0.2.076 → 0.2.077（作用于全部项目）。
- 本阶段（93）仅改 `docs/` 下 markdown，零代码改动，无需再次 bump。
- 当前版本：0.2.077（`projects/demo-a2/config.toml`、`projects/animatest/config.toml` 同步）。

## §七 变更文件清单（本阶段）

| 文件 | 变更 |
|------|------|
| `docs/B92-NB脚本命令参考.md` | +`cg` 命令行；`cgvmenu` 从 TODO 改已实现（模块更正为 nb_mainmenu.c）；管线"五表"→"六表 + CG_COUNT" |
| `docs/B90-参考-函数索引.md` | `cmd_cgvmenu` 标注实现状态；+`cmd_cg` 行；`sys_save_unlock_cg`/`is_cg_unlocked` 从"内部 static"移入公开 API |
| `docs/B18-存档读档系统设计.md` | §2.4 表格行号区间 `290–333` 过期修正为逐行精确行号；补列 `sys_save_unlock_cg` / `sys_save_is_cg_unlocked`（§五 复查补修） |
| `devdocs/93-CG画廊-文档收口.md` | 本档（含 §五 B18 补修记录与 devdoc 92 偏差登记） |

无任何 `.c` / `.h` / `.py` / `.sh` / `.nb` 源码改动。

## §八 验证

由于本阶段仅改 markdown，不触发 make / pytest / fullaudit（它们审计源码）。验证方式为人工核对：

1. `grep -n "cg\|cgvmenu" docs/B92-NB脚本命令参考.md` → `cg`、`cgvmenu` 两行均已实现描述。
2. `grep -n "sys_save_unlock_cg\|sys_save_is_cg_unlocked" docs/B90-参考-函数索引.md` → 位于公开 API 段，非"内部 static"段。
3. `grep -n "CG_COUNT\|六表" docs/B92-NB脚本命令参考.md` → 管线描述已更新。
4. `grep -n "sys_save_unlock_cg\|sys_save_is_cg_unlocked\|sys_save_unlock_scene" docs/B18-存档读档系统设计.md` → 表格已列 CG 解锁/查询函数且无 `290–333` 过期区间。
5. 确认全 repo 无源码文件在本阶段被改动（`git status` 仅见 docs 与 devdocs）。

## §九 画廊功能总收口

CG 画廊功能（7 阶段）全部落地完成：

- **数据层**：`type='CG'` 资产 → `IMAGE.DAT` → `export_asset_table` 生成 `cg_map[]` + `CG_COUNT`。
- **存档层**：`sys_save_unlock_cg`（`cmd_cg` 触发）、`sys_save_is_cg_unlocked`（画廊判定）。
- **展示层**：`cg <key>` 命令走完整 `layer_bg_change` 渲染路径并永久解锁。
- **画廊层**：主菜单 gallery → `cgview.nb` → `cmd_cgvmenu`（4×3 网格 + 锁定占位 + 翻页 + 全屏预览 + 双层 ESC 退出）。
- **文档层**：devdoc 88–93 六份（含本收口档）；B92/B90 参考文档同步。

二期待办（**执行入口见 devdoc 94 §四**，原 devdoc 92 §八声明）：真缩略图（当前为编号/占位框），网格布局与导航无需改动即可升级。

> 另有**未完成挂起项**：NP2kai 实机手动验收（devdoc 92 §七 步骤 6）——执行与核销转 `devdocs/94-CG画廊-未完成部分与实机验收.md`。
